{
  "name": "offline-accounting",
  "version": "1.0.0",
  "description": "离线记账应用 - 无需网络",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:init": "npx cap init 记账本 com.example.offlineaccounting --web-dir=dist",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:build": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:open": "npm run build && npx cap sync android && npx cap open android"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/cli": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  }
}
